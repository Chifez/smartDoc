/**
 * @description This is a test function to show how jsDoc is used
 * @param num1 - this is a parameter that is of type number
 * @param num2 - this is also a parameter that is of type number
 * @returns - this returns a number
 */

export const testFunc = (num1: number, num2: number) => {
  return num1 * num2;
};
